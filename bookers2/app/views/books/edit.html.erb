<main>
  <div class= "container">
    <div class="row">
      <div class="mx-auto">
<%= form_with model:@book, local:true do |f| %>


<h1>Editing Book</h1>

<div class="error">
  <% if @book.errors.any? %>
<h3><%= pluralize(@book.errors.count, "error") %> errors prohibited this obj from being saved: </h3>
  <% @book.errors.full_messages.each do |message| %>
  <ul><%= message %></ul>
  <% end %>
  <% end %>
</div>

<h5>Title</h5>
<%= f.text_field:title, size:30 %>

<h5>Opinion</h5>
<%= f.text_area:body, size:"30x10" %>

<div class="form-group">
  <input type="submit" name="commit" value="Update Book" class="btn btn-secondary" data-disable-with="Update Book">
</div>


<% end %>

    <%= link_to "Show |", book_path(@book) %>
    <%= link_to " Back", books_path %>
   </div>
  </div>
 </div>
</main>